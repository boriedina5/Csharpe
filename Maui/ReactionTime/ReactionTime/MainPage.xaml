<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:ReactionTime.ViewModel"
             x:Class="ReactionTime.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Label
                Text="{Binding StatusText}" FontSize="28"
                HorizontalOptions="Center" VerticalOptions="Center"
                TextColor="White"/>
            <Button Text="START" Command="{Binding StartGameCommand}"/>
            <Button Text="KATTINTS" Command="{Binding GetReactionTimeCOmmand}" FontSize="22"/>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
